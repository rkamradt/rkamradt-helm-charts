apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: {{ .Release.Namespace }}
  labels:
    app: loki
spec:
  type: {{ .Values.service.type }}
  selector:
    app: loki
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3100
      protocol: TCP
