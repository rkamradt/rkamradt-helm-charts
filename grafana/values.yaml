replicaCount: 1

image:
  repository: grafana/grafana
  tag: "10.4.2"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

auth:
  adminUser: admin
  adminPassword: admin

resources:
  requests:
    memory: "128Mi"
    cpu: "50m"
  limits:
    memory: "256Mi"
    cpu: "200m"
