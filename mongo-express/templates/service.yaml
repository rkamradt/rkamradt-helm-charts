apiVersion: v1
kind: Service
metadata:
  name: mongo-express
  namespace: {{ .Release.Namespace }}
  labels:
    app: mongo-express
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mongo-express
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8081
      protocol: TCP
